# Генератор актов Wildberries

Простое десктопное приложение для автоматической генерации актов из XLSX файлов с данными о продажах на Wildberries.

## Что делает приложение?

1. Принимает XLSX файл, где каждая вкладка — это месяц продаж
2. Позволяет выбрать месяц
3. Генерирует два документа в формате DOCX:
   - **Акт приема-передачи товара** — с таблицей всех товаров
   - **Акт выполненных услуг** — с общей информацией и суммами

## 🚀 Быстрый запуск (РЕКОМЕНДУЕТСЯ)

### Для новичков - просто двойной клик!

1. **Установите Python** с [python.org](https://www.python.org/downloads/) (версия 3.8 или выше)
   - Важно: при установке отметьте галочку "Add Python to PATH"

2. **Запустите приложение** двойным кликом на файл:
   - **macOS**: `Запустить приложение.command` или `start.py`
   - **Windows**: `start.py`

3. **При первом запуске** программа автоматически установит все необходимые компоненты

4. **Откроется окно приложения** - можно работать!

Вот и всё! Никаких терминалов и сложных команд.

---

## 📋 Альтернативный способ (через терминал)

Если у вас опыт работы с терминалом, можете запустить вручную:

### Шаг 1: Установите зависимости

```bash
cd wildberries-acts-generator
pip3 install -r requirements.txt
```

### Шаг 2: Запустите приложение

**Вариант 1 - Десктопное GUI (рекомендуется):**
```bash
python3 gui_app.py
```
Откроется отдельное окно приложения с графическим интерфейсом (БЕЗ браузера)

**Вариант 2 - Веб-интерфейс в окне:**
```bash
python3 desktop_app.py
```
Откроется окно с веб-интерфейсом

**Вариант 3 - Веб-версия в браузере:**
```bash
python3 app.py
```
Затем откройте браузер на `http://localhost:5000`

## Как использовать

1. **Загрузите XLSX файл**
   - Нажмите "Выбрать файл" или перетащите файл на область загрузки
   - Файл должен содержать вкладки с названиями месяцев

2. **Выберите месяц**
   - После загрузки появится список всех месяцев из файла
   - Каждый месяц представлен карточкой с двумя кнопками

3. **Скачайте документы**
   - Нажмите "Акт приема-передачи" для скачивания акта с таблицей товаров
   - Нажмите "Акт услуг" для скачивания акта выполненных услуг

## Структура проекта

```
wildberries-acts-generator/
│
├── start.py                          # 🚀 ЗАПУСТИТЕ ЭТО для десктопного приложения
├── Запустить приложение.command      # 🚀 Для macOS (двойной клик)
├── gui_app.py                        # 🎨 GUI приложение (БЕЗ браузера)
├── desktop_app.py                    # Веб-интерфейс в окне
├── app.py                            # Flask сервер для браузера
├── requirements.txt                  # Список зависимостей
├── README.md                         # Эта инструкция
│
├── templates/                        # HTML шаблоны
│   └── index.html                    # Главная страница
│
├── uploads/                          # Загруженные файлы (создается автоматически)
├── generated/                        # Сгенерированные документы (создается автоматически)
└── static/                           # Статические файлы (создается автоматически)
```

## Требования к XLSX файлу

- Файл должен быть в формате `.xlsx`
- Каждая вкладка представляет отдельный месяц
- Название вкладки будет использовано как название месяца
- Первая строка каждой вкладки — заголовки столбцов
- Остальные строки — данные

Пример структуры:

**Вкладка "Январь 2024":**
| Артикул | Название товара | Количество | Цена | Сумма |
|---------|-----------------|------------|------|-------|
| 123456  | Товар 1        | 5          | 1000 | 5000  |
| 123457  | Товар 2        | 3          | 1500 | 4500  |

## Остановка приложения

**Десктопное приложение:** Просто закройте окно приложения

**Веб-версия:** Нажмите `Ctrl+C` в терминале, где запущено приложение

## Частые вопросы

**Q: Что делать, если появляется ошибка "Address already in use"?**

A: Порт 5000 уже занят другим приложением. Измените порт в файле `app.py` (последняя строка):
```python
app.run(debug=True, host='0.0.0.0', port=5001)  # Измените 5000 на 5001
```

**Q: Можно ли изменить формат документов?**

A: Да, вы можете отредактировать функции `generate_acceptance_act()` и `generate_services_act()` в файле `app.py`. Там можно изменить текст, форматирование и структуру документов.

**Q: Где хранятся загруженные файлы?**

A: Загруженные файлы сохраняются в папке `uploads/`, а сгенерированные документы — в папке `generated/`. Эти папки создаются автоматически при первом запуске.

**Q: Как настроить внешний вид страницы?**

A: Откройте файл `templates/index.html` и измените CSS стили в секции `<style>`.

## Безопасность

⚠️ **Важно!** Этот сервис предназначен для локального использования. Если вы планируете развернуть его в интернете:

1. Измените `app.secret_key` в файле `app.py` на случайную строку
2. Отключите режим отладки: `app.run(debug=False)`
3. Используйте production WSGI сервер (например, Gunicorn)
4. Настройте аутентификацию пользователей

## Поддержка

Если возникли проблемы:
1. Убедитесь, что установлена правильная версия Python (3.8+)
2. Проверьте, что все зависимости установлены: `pip3 list`
3. Проверьте формат XLSX файла
4. Посмотрите сообщения об ошибках в терминале

---

Приятного использования! 🚀
